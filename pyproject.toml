[tool.poetry]
name = "brightwheel-challenge"
version = "0.1.0"
description = ""
authors = ["Victor Lucio"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pandas = "^2.1.4"
loguru = "^0.7.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
